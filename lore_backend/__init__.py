"""Lore backend — an engineering team's decision memory.

The distribution version lives here and nowhere else: pyproject reads it at
build time, and `main.py` stamps it onto the FastAPI app.
"""

__version__ = "0.1.1"
